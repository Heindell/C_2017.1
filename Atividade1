#include <stdio.h>
#include <stdlib.h>

	typedef struct elemeno{
		char nome[50];
	}t_elemento;
	
	typedef struct lista{
		t_elemento vetor[4];
		int n; //a ultima posiçao ocupada
	}t_lista;
	
t_lista criar(){
	t_lista lista;
	
	lista.n = -1;
	
	return lista;
}

int insere(t_lista * l, char *){
	strcpy(1->mLista.vetor[l->++n].nome, s);
	l->n += 1
	
	
	return 1;
}
int main(){
	int i;
	char str[50];
	//criar lista
	t_lista mLista = criar();
	
	//guardar 3 nomes
	for (i = 0; i < 3; i++){
		
		scanf("%s", &str);
		insere(&mLista, str);
		
	}
	//buscar e mostrar um nome
	
	//apagar um nome
	
	
	
}
